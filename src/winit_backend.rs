use crate::{MainLoop, AppInfo};
use anyhow::Result;

pub fn launch<M: MainLoop>(info: AppInfo) -> Result<()> {
    todo!()
}

// TODO: Functions that launch with other info (layers, extensions, title, etc.)?
